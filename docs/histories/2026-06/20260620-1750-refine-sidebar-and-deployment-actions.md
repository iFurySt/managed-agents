# Refine Sidebar And Deployment Actions

- Compared Claude Console source and local deployments action menus with Open Browser Use.
- Replaced deployment Resume/Edit menu entries with matching Anthropicons glyphs and aligned Archive with the source red action color.
- Narrowed the expanded console sidebar and removed the extra brand text offset so the sidebar header aligns closer to the Claude Console reference.
- Verified with `npm run build --workspace apps/console`, `go test ./...`, and `git diff --check`.
