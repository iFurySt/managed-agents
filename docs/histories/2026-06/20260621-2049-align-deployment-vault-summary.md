# Align Deployment Vault Summary

## Request

Continue converging the Claude Platform managed agents console clone using OBU and visible source evidence, while keeping changes narrowly scoped.

## Changes

- Updated the deployment vault picker summary for `test_secret` from `3 credentials` to `5 credentials`.
- Kept the change aligned with the source vault detail evidence showing five credentials in `test_secret`.

## Files

- `apps/console/src/App.tsx`
