# Align memory store created labels

## Request

Continue converging the Claude Console clone against source platform evidence without expanding scope.

## Changes

- Updated memory store seed labels to match the current Claude Platform memory stores table.
- Added a focused apiserver seed test to lock the source-matched created labels for `123`, `zzz`, `world cup`, and `leo_test`.

## Files

- `apps/apiserver/main.go`
- `apps/apiserver/main_test.go`
