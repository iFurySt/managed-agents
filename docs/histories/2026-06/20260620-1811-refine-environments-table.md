# Refine Environments Table

- Compared Claude Console and local Environments pages with Open Browser Use at a 2048px viewport.
- Added the Environments header documentation icon button and linked it to the matching Managed Agents environments docs URL.
- Matched the Environments table width and column geometry to the source layout: 1552px table width with ID, Name, Status, Type, Updated at, and actions columns aligned.
- Verified with Open Browser Use DOM geometry checks, `npm run build --workspace apps/console`, and `go test ./...`.
