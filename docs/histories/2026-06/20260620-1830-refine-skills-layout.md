# Refine Skills Layout

- Compared Claude Console and local Skills pages with Open Browser Use at a 2048px viewport.
- Centered the Skills page on a 1216px content column to match the source layout.
- Expanded the skills list to the full source column width so the header actions and row action button align with Claude Console.
- Verified with Open Browser Use DOM geometry checks, `npm run build --workspace apps/console`, and `go test ./...`.
